

CREATE FILTER MODULE get_Filter
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		DECLARE AccountID INTEGER CAST(Environment.Variables.AccountID AS INTEGER);
		IF AccountID < 5000 THEN
			RETURN FALSE;
		ELSE
			RETURN TRUE;
		END IF;
	END;

END MODULE;
